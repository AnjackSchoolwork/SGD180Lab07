
// Input and control configuration

var move_left = K_W
var move_right = K_D
var move_up = K_SPACE

function checkKeys() {
	for (var index in keysDown) {
		if (keysDown[index] == move_left) {

		}
		else if (keysDown[index] == move_right) {

		}
		else if (keysDown[index] == move_up) {

		}
	}
}


function adjustVector2d(target, magnitude) {
	// Adjust vector by magnitude
}